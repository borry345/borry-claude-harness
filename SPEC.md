# 할 일 관리 프로그램 (터미널) 스펙

## 1. 개요 (Overview)

터미널(CLI 환경)에서 실행되는 할 일(To-Do) 관리 프로그램을 만든다. 프로그램을 실행하면 종료 전까지 계속 떠 있는 대화형 메뉴 방식으로 동작하며, 사용자는 메뉴를 통해 할 일을 새로 추가하고, 전체 목록을 조회하고, 목록 번호로 특정 할 일을 완료 처리하고, 목록 번호로 특정 할 일을 삭제할 수 있어야 한다. 각 할 일은 추가 시점에 `YYYY-MM-DD` 형식의 마감일을 함께 지정할 수 있으며, 목록을 조회할 때 마감일이 이미 지난(기한이 초과된) 미완료 항목은 텍스트 라벨(예: `[지연]`)로 다른 항목과 구분되어 표시되어야 한다. 할 일 데이터는 파일에 저장되어 프로그램을 재시작해도 목록이 유지되어야 한다.

## 2. 기능 목록 (Feature List)

| ID | 기능명 |
|----|--------|
| F1 | 할 일 추가 (Add Task) |
| F2 | 할 일 목록 조회 (List Tasks) |
| F3 | 할 일 완료 처리 (Complete Task) |
| F4 | 할 일 삭제 (Delete Task) |
| F5 | 마감일 지정 (Set Due Date) |
| F6 | 마감일 초과 항목 강조 표시 (Highlight Overdue Tasks) |

## 3. 기능별 요구사항 및 수용 기준 (Functional Requirements & Acceptance Criteria)

### F1. 할 일 추가 (Add Task)

**요구사항**
사용자는 대화형 메뉴를 통해 새로운 할 일을 추가할 수 있어야 한다. 할 일은 한 줄짜리 제목(문자열)만 가지며, 별도의 상세 설명 필드는 없다. 추가 시 마감일을 `YYYY-MM-DD` 형식으로 함께 지정할 수 있으며(F5 참고), 마감일은 추가 시점에만 지정할 수 있다. 동일한 내용(제목)의 할 일이 이미 있어도 중복 추가를 허용한다. 추가된 할 일은 목록 번호가 부여되어 목록 조회(F2)에서 확인할 수 있고, 파일에 저장되어 프로그램을 재시작해도 유지되어야 한다.

**수용 기준**
- 사용자가 제목을 입력하여 추가를 실행하면, 해당 할 일이 목록에 새 항목으로 나타나야 한다.
- 새로 추가된 할 일의 초기 완료 상태는 "미완료"여야 한다.
- 제목 없이(빈 값으로) 추가를 시도하면, 추가가 거부되고 사용자에게 오류가 안내되어야 한다.
- 마감일을 지정하지 않고 추가한 할 일은 마감일이 없는 상태로 목록에 나타나야 한다.
- `YYYY-MM-DD` 형식의 유효한 마감일을 지정하여 추가한 할 일은, 지정한 마감일 값이 목록 조회 시 그대로 표시되어야 한다.
- 이미 등록된 할 일과 제목이 완전히 동일한 새 할 일을 추가해도, 오류 없이 별개의 항목으로 추가되어야 한다.
- 할 일을 추가한 뒤 프로그램을 종료했다가 다시 실행하면, 방금 추가한 할 일이 목록 조회 시 그대로 남아 있어야 한다.

### F2. 할 일 목록 조회 (List Tasks)

**요구사항**
사용자는 대화형 메뉴를 통해 현재 등록된 모든 할 일을 조회할 수 있어야 한다. 목록의 각 항목은 완료 처리(F3)·삭제(F4) 시 지정에 사용할 목록 번호, 제목, 완료 여부, 마감일(있는 경우)을 표시해야 하며, 항목은 등록된 순서대로 나열되어야 한다. 완료된 항목과 미완료 항목은 구분 없이 하나의 목록에 함께 표시하고 완료 여부 표기로만 구분하며, 별도 구역 분리나 숨김 옵션은 제공하지 않는다. 마감일이 지난 미완료 항목은 F6 규칙에 따라 강조 표시되어야 한다.

**수용 기준**
- 할 일이 하나 이상 등록된 상태에서 목록 조회를 실행하면, 등록된 모든 할 일이 각각 한 줄(또는 한 항목)씩, 등록된 순서 그대로 표시되어야 한다.
- 등록된 할 일이 하나도 없는 상태에서 목록 조회를 실행하면, "할 일이 없다"는 취지의 안내가 표시되고 오류 없이 정상 종료되어야 한다.
- 각 항목에는 완료 처리(F3)·삭제(F4)에 사용할 수 있는 목록 번호가 함께 표시되어야 한다.
- 목록의 각 항목에서 완료 여부(완료/미완료)를 시각적으로 구분할 수 있어야 한다.
- 완료된 항목과 미완료 항목이 모두 있을 때, 두 종류가 동일한 하나의 목록 안에 등록 순서대로 함께 나타나야 한다(별도 구역으로 분리되지 않는다).
- 마감일이 지정된 항목은 목록에 마감일 값(`YYYY-MM-DD`)이 함께 표시되어야 한다.
- 마감일이 지정되지 않은 항목은 마감일이 없다는 것을 알 수 있는 형태(예: 빈 값 또는 별도 표기)로 표시되어야 한다.
- 프로그램을 재시작한 직후에도, 이전에 저장된 할 일 목록이 그대로 조회되어야 한다.
- 저장된 데이터 파일이 아직 없는 최초 실행 상태에서 목록 조회를 실행해도, 오류 없이 "할 일이 없다"는 안내가 표시되어야 한다.

### F3. 할 일 완료 처리 (Complete Task)

**요구사항**
사용자는 목록 조회(F2)에서 표시된 목록 번호를 지정하여 해당 할 일을 완료 상태로 변경할 수 있어야 한다. 이미 완료 상태인 항목에 완료 처리를 다시 실행해도 오류 없이 정상 처리되며(idempotent), 상태에는 변화가 없다. 완료 처리 결과는 파일에 저장되어 프로그램을 재시작해도 유지되어야 한다.

**수용 기준**
- 미완료 상태인 할 일의 번호를 지정해 완료 처리하면, 이후 목록 조회 시 해당 항목이 완료 상태로 표시되어야 한다.
- 존재하지 않는 목록 번호(범위를 벗어나거나 형식이 잘못된 입력 등)로 완료 처리를 시도하면, 오류가 안내되고 다른 항목의 상태는 변경되지 않아야 한다.
- 이미 완료 상태인 할 일의 번호를 지정해 완료 처리를 다시 실행하면, 오류 없이 처리되고 해당 항목은 계속 완료 상태로 남아 있어야 한다(상태가 "미완료"로 되돌아가지 않는다).
- 완료 처리한 뒤 프로그램을 종료했다가 다시 실행하면, 목록 조회 시 해당 항목이 계속 완료 상태로 표시되어야 한다.

### F4. 할 일 삭제 (Delete Task)

**요구사항**
사용자는 목록 조회(F2)에서 표시된 목록 번호를 지정하여 해당 할 일을 삭제할 수 있어야 한다. 완료 여부와 관계없이 삭제할 수 있다. 삭제 결과는 파일에 저장되어 프로그램을 재시작해도 유지되어야 한다.

**수용 기준**
- 존재하는 할 일의 번호를 지정해 삭제하면, 이후 목록 조회 시 해당 항목이 더 이상 나타나지 않아야 한다.
- 존재하지 않는 목록 번호(범위를 벗어나거나 형식이 잘못된 입력 등)로 삭제를 시도하면, 오류가 안내되고 다른 항목은 삭제되지 않아야 한다.
- 삭제는 완료 여부와 관계없이(완료된 항목이든 미완료 항목이든) 수행할 수 있어야 한다.
- 삭제한 뒤 프로그램을 종료했다가 다시 실행하면, 목록 조회 시 삭제한 항목이 나타나지 않아야 한다.

### F5. 마감일 지정 (Set Due Date)

**요구사항**
사용자는 할 일 추가(F1) 시점에 `YYYY-MM-DD` 형식의 마감일을 지정할 수 있어야 한다. 마감일 지정은 추가 시점에만 가능하며, 이미 등록된 할 일의 마감일을 이후에 추가·수정·제거하는 기능은 이번 범위에 포함하지 않는다.

**수용 기준**
- 추가 시 `YYYY-MM-DD` 형식의 유효한 마감일을 입력하면, 해당 할 일에 마감일이 정상적으로 저장되고 목록 조회 시 표시되어야 한다.
- 추가 시 `YYYY-MM-DD` 형식에 맞지 않는 마감일(예: 존재하지 않는 날짜, 다른 구분자 사용, 문자 혼입 등)을 입력하면, 입력이 거부되고 사용자에게 오류가 안내되어야 한다.
- 마감일을 지정하지 않고 추가한 할 일은 마감일 없는 상태로 유지되며, F6의 마감일 초과 판정 대상에서 제외되어야 한다.
- 이미 등록된 할 일에 대해 마감일을 나중에 추가, 변경, 제거하는 기능은 제공되지 않는다(메뉴에 해당 기능이 없어야 한다).

## 5. 구현 현황 (Implementation Status)

- 통합 대상 코드: `todo-cli/taskStore.js` (데이터 계층, implementer-A), `todo-cli/index.js` (대화형 메뉴, implementer-B)
- 통합 담당: team-lead · 검증 담당: tester (2026-09-11)
- 통합 노트: 두 구현이 동일한 파일(`taskStore.js`)에 동시 write하면서 경쟁이 있었으나(RUN_LOG.md 참고), 최종적으로 남은 것은 implementer-A의 실제 구현이며 implementer-B의 `index.js`가 기대하는 함수 시그니처(`loadTasks`, `saveTasks`, `addTask`, `completeTask`, `deleteTask`, `isOverdue`)와 태스크 객체 형태(`{title, done, dueDate}`)가 실제로 정확히 일치함을 team-lead가 직접 실행 스모크 테스트로 재확인함(자기 보고에 의존하지 않음). 별도의 통합 수정(충돌 해결, 와이어링 추가)은 필요하지 않았음. 별도 entrypoint/README 불필요 — `node index.js`로 바로 실행 가능.

| ID | 기능명 | 상태 | 검증 방법 |
|----|--------|------|-----------|
| F1 | 할 일 추가 (Add Task) | ✅ 완료(PASS) | tester: 단위 테스트 9건 + CLI 구동 시나리오, 재시작 영속성 포함 |
| F2 | 할 일 목록 조회 (List Tasks) | ✅ 완료(PASS) | tester: 빈 목록/최초 실행/등록 순서/재시작 영속성 시나리오 |
| F3 | 할 일 완료 처리 (Complete Task) | ✅ 완료(PASS) | tester: 단위 테스트 5건, 재완료 idempotent 및 잘못된 번호 처리 포함 |
| F4 | 할 일 삭제 (Delete Task) | ✅ 완료(PASS) | tester: 단위 테스트 5건, 완료/미완료 삭제 및 잘못된 번호 처리 포함 |
| F5 | 마감일 지정 (Set Due Date) | ✅ 완료(PASS) | tester: 날짜 형식 검증(유효/무효), 메뉴에 수정 기능 없음 확인 |
| F6 | 마감일 초과 항목 강조 표시 (Highlight Overdue Tasks) | ✅ 완료(PASS) | tester: 경계값 5종 시나리오(지연/완료제외/오늘/미래/마감없음) + ANSI 색상 미사용 확인 |

**tester 실행 로그**: RUN_LOG.md 참고 — F1~F6 전체 1회차 호출로 전건 PASS, 재호출(3-strikes) 불필요.
**미구현/커버리지 갭**: 없음 (스펙의 모든 기능에 대해 implementer 산출물이 존재하고 tester 검증을 통과함).

### F6. 마감일 초과 항목 강조 표시 (Highlight Overdue Tasks)

**요구사항**
목록 조회(F2) 시, 마감일이 지정되어 있고 그 마감일이 오늘 날짜보다 이전이며 아직 완료되지 않은 할 일은 텍스트 라벨(예: `[지연]`)을 붙여 다른 항목과 구분되게 표시해야 한다. 색상에 의존하지 않고 터미널의 색상 지원 여부와 무관하게 항상 동일하게 표시되어야 한다. 마감일이 오늘 날짜와 같은 경우는 아직 지나지 않은 것으로 간주한다(해당 날짜 자정까지는 초과로 보지 않는다). 완료된 항목은 마감일이 지났더라도 강조 표시 대상에서 제외한다.

**수용 기준**
- 마감일이 오늘 날짜보다 이전이고 완료되지 않은 할 일은 목록에서 텍스트 라벨(예: `[지연]`)이 함께 표시되어야 한다.
- 마감일이 오늘 날짜와 정확히 같은 할 일은 강조 표시(라벨) 없이 일반 항목으로 표시되어야 한다.
- 마감일이 오늘 날짜보다 이후인 할 일은 강조 표시 없이 일반 항목으로 표시되어야 한다.
- 마감일이 없는 할 일은 강조 표시 대상이 아니어야 한다.
- 마감일이 오늘보다 이전인 할 일이라도, 완료 처리된 상태라면 강조 표시(라벨)가 붙지 않아야 한다.
- 강조 표시는 색상이 아닌 텍스트 라벨로만 이루어져야 하며, 색상 출력을 지원하지 않는 터미널에서도 동일하게 식별 가능해야 한다.

## 4. 미확정 사항 (Open Questions)

이전에 식별된 13개의 미확정 사항은 사용자 확인(실행 방식, 할 일 식별 방법, 데이터 영속성, 마감일 형식) 및 PM의 MVP 단순화 기본 결정(마감일 변경/삭제 범위 제외, 완료 재처리 idempotent 처리, 완료 항목의 강조 표시 제외, "오늘" 경계 처리, 완료/미완료 통합 표시, 등록 순서 정렬, 텍스트 라벨 방식의 강조 표시, 제목 전용 데이터 구조, 중복 허용)를 통해 모두 해소되어 본 문서에 반영되었다.

현재 시점 기준으로 구현을 가로막는 미확정 사항은 없다. 다만 아래 사항은 PM의 기본 결정에 따른 것으로, 향후 사용자 요청에 따라 언제든 변경될 수 있는 항목임을 참고용으로 남긴다(재확인이 필요하면 다시 논의):

- 마감일 변경/삭제 기능을 이후 범위에 포함할지 여부 (현재: 미포함)
- 완료된 항목을 별도 구역으로 분리하거나 숨기는 옵션을 추가할지 여부 (현재: 미제공, 통합 표시)
- 강조 표시에 색상을 추가로 지원할지 여부 (현재: 텍스트 라벨만 사용)
- 할 일에 제목 외 추가 필드(설명 등)를 허용할지 여부 (현재: 제목만 허용)
- 중복 할 일에 대한 경고/검증을 추가할지 여부 (현재: 검증 없이 허용)
