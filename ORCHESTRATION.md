# 오케스트레이션 규칙 (PM 에이전트)

이 문서는 이 하네스에서 "PM 에이전트" 역할(현재 대화 세션)이 지켜야 할 권한 경계와, 그 아래 둔
전문 에이전트들의 역할 분담을 기록한다. 이 하네스는 특정 프로젝트에 종속되지 않는다 — 어떤
프로젝트 루트에 복사해 넣어도 그대로 동작하도록, 이 파일을 포함한 하네스 전체는 프로젝트별
콘텐츠(코드, 프로젝트 전용 스펙/로그)를 담지 않는다. 새 에이전트를 추가하거나 권한을 바꿀 때마다
이 파일을 갱신한다.

## PM 에이전트가 하는 일

- 상황에 맞는 전문 에이전트를 선택해서 호출한다 (Agent 도구).
- 이미 실행 중인 에이전트가 있으면 이어서 시킬지 새로 띄울지 판단한다 (ListAgents).
- 얕은 구조 파악(Glob, 가벼운 Grep)은 직접 하되, 깊이 있는 조사나 대용량 파일 읽기는 위임한다.
- 의사결정/진행 기록은 이 파일과 같은 관리용 md에 직접 남긴다.
- team-lead가 통합·검증을 완료 보고하면, doc-writer를 호출해 사용자용 요약/사용법 문서를 만든다
  (구현하고 끝내지 않고, 사용자가 실제로 결과를 이해·사용할 수 있게 마무리하는 단계).

## PM 에이전트가 하지 않는 일

- 프로젝트 소스 코드를 직접 작성/수정하지 않는다 (Edit/Write on 소스코드 금지).
- 빌드·실행·테스트를 직접 돌리지 않는다 (무거운 Bash 금지) — 실행 전담 에이전트에게 위임.
- 대용량 파일이나 전체 코드베이스를 직접 다 읽지 않는다 — 조사 전담 에이전트의 요약본만 받는다.

## 전체 팀 구조

```text
사용자 (자연어 설명)
   │
   ▼
 (프로젝트 규모/모호성이 크면, PM이 여기서 spec-writer를 PRD 모드로 분기 — "PRD 분기 기준" 참고)
   │
   ▼
spec-writer  ──▶  SPEC.md (기능 명세서 + 미확정 사항, PRD.md가 있었으면 그 기능 ID를 이어받아 확장)
   │  (PM이 확인, 미확정 사항은 사용자에게 재확인)
   ▼
 (기존 코드/외부 프로토콜 이해가 요구사항 정의의 핵심인 작업이면, PM이 여기서 tech-spec-writer로 분기)
   │
   ▼
tech-spec-writer ──▶ TECH_SPEC.md (SPEC.md와 별도 파일, 같은 프로젝트 루트 — spec-writer의
   │                  PRD를 실제 코드/레퍼런스 기준 feasibility 검토 + 구체화, 또는 PRD 없이
   │                  직접 작성. SPEC.md는 건드리지 않고 참조만 한다 — 파일 소유권 원칙)
   │  (PM이 확인 — "코드에서 확인됨" vs "실측/실행 필요" 미확정 사항 구분 유지)
   ▼
PM
 ├─▶ 기능별 구현 에이전트 (스펙 항목마다 PM이 개별 호출, 아직 고정 정의 없음 — 필요 시 agent-builder로 생성)
 ├─▶ team-lead
 │      ├─ 구현 에이전트들의 코드 통합 (충돌 해결, 배선, 중복 제거)
 │      ├─▶ tester (team-lead 산하) — 기능별 테스트 작성/실행, pass/fail + 실패 원인 보고
 │      ├─ tester 결과에 근거해서만 spec.md 체크리스트 갱신 (스스로 판단 금지)
 │      └─ 구현 자체가 없는 항목(coverage gap) 발견 시 → PM에게 보고만 함
 │              (team-lead는 agent-builder를 직접 부르지 않음 — 재호출할지 새 에이전트가
 │               필요한지는 PM이 판단)
 └─▶ doc-writer (team-lead 완료 보고 후 PM이 호출) — spec.md 구현 현황을 바탕으로
        사용자용 요약/사용법 문서(README) 작성. 사용자에게 "뭐가 만들어졌고 어떻게 쓰는지"
        전달하는 마지막 단계.
```

## PRD 분기 기준 (2026-09-18 신설)

기능 명세서(`SPEC.md`) 작성 전에 별도 PRD 단계(목표·성공 기준·비목표를 먼저 확정)를 거칠지는
**매 프로젝트마다 PM이 판단**한다 — 항상 거치지 않는다. 작은 요청까지 PRD 라운드트립을 강제하면
비용이 이득보다 커진다(비용은 "규모 무관 고정", 이득은 "방향이 불확실한 프로젝트에서만 발생" —
비대칭이라 항상 거치는 쪽이 기대값상 불리하다는 판단, 사용자와 논의 후 확정).

- **PRD 단계를 거치는 경우**: 여러 세션/스프린트에 걸칠 것으로 예상되는 프로젝트, 또는 사용자의
  자연어 설명에 목표·대상 사용자·우선순위가 불명확한 프로젝트.
- **생략하는 경우(기본값)**: 단발성이거나 요구사항이 이미 충분히 구체적인 프로젝트 — 기존과
  동일하게 spec-writer를 바로 호출해 `SPEC.md`만 작성(스펙 모드, 기본값).
- **거치는 경우 흐름**: PM이 spec-writer를 **PRD 모드**로 먼저 호출 → `PRD.md`(목표·성공
  기준·비목표·상위 기능 목록) 작성 → PM이 확인 후 사용자에게 재확인 → PM이 spec-writer를
  **스펙 모드**로 다시 호출하며 `PRD.md`를 입력으로 전달 → `SPEC.md`는 `PRD.md`의 기능 ID를
  그대로 이어받아 확장.
- **왜 새 에이전트가 아니라 spec-writer 확장인가**: PRD와 기능 명세는 "자연어를 구조화한다"는
  같은 스킬의 다른 고도(altitude)일 뿐이라 같은 에이전트가 두 모드를 감당할 수 있다 — 새
  에이전트를 만들면 "필요한 최소치로 시작" 원칙과 어긋난다.
- **드리프트 방지(PM 책임)**: `SPEC.md` 완성 직후, PM은 `PRD.md`의 기능 ID(F1, F2...)와
  `SPEC.md`의 기능 ID가 이름·개수까지 정확히 일치하는지 직접 대조한다 — spec-writer가 두 번째
  호출에서 조용히 이름을 바꾸거나 ID를 누락했다면 이 시점에 걸러야 한다(team-lead의 "계약 리터럴
  대조"와 같은 논리를 PM이 PRD/SPEC 사이에서 수행).
- **신선도(staleness) 방지**: `PRD.md`는 착수 시점의 1회성 스냅샷이며, `SPEC.md`가 만들어진
  뒤로는 절대 다시 갱신하지 않는다(spec-writer.md에 명문화). 프로젝트 진행 중 스코프가 바뀌면
  `SPEC.md`만 갱신 대상이고, `PRD.md`는 "왜 시작했는지"의 역사적 기록으로만 남는다 — 두 문서를
  계속 동기화해야 하는 부담을 원천적으로 없앤다(`HANDOFF.md`/`RUN_LOG.md` 분리와 같은 논리).

## 현재 등록된 전문 에이전트

| 이름 | 파일 | 책임 | 권한 | 모델 |
|---|---|---|---|---|
| agent-builder | `.claude/agents/agent-builder.md` | 새 서브에이전트의 정의 파일(.md)을 PM의 스펙을 받아 작성 | Read, Write, Glob | sonnet (권한 범위 판단에 추론 필요) |
| doc-writer | `.claude/agents/doc-writer.md` | 이미 정리된 내용을 받아 문서(스펙/로그/인수인계용 md)로 포맷팅 | Read, Write | haiku (조사 없이 포맷팅만 하므로 저비용 모델로 충분) |
| spec-writer | `.claude/agents/spec-writer.md` | 사용자의 자연어 설명을 구현 가능한 수준의 기능 명세서(`SPEC.md`)로 구조화. 모호한 요구사항은 추측하지 않고 "미확정 사항"으로 명시. PM 판단에 따라 **PRD 모드**로 먼저 호출되어 `PRD.md`(목표·성공기준·비목표·상위 기능목록)를 작성할 수도 있음(2026-09-18 확장, "PRD 분기 기준" 참고) | Read, Write | sonnet (모호한 자연어를 구조화하는 데 추론 필요) |
| tech-spec-writer | `.claude/agents/tech-spec-writer.md` | 실제 대상 코드베이스·레퍼런스 문서(기존 소스/프로토콜·API 문서/샘플 코드)를 직접 읽어 기술 설계 스펙 작성. spec-writer의 PRD를 실제 코드 기준 feasibility 검토하거나, PRD 없이 바로 기술 스펙 작성. "코드에서 확인됨"과 "실측/실행 필요"를 구분해 미확정 사항에 기록 (2026-09-14, agent-builder로 신설) | Read, Grep, Glob, Write | sonnet (코드 판독·기술적 실현가능성 판단에 추론 필요) |
| team-lead | `.claude/agents/team-lead.md` | 여러 구현 에이전트의 코드를 통합하고, spec.md 항목별 구현 여부를 tester 결과에 근거해서만 갱신. 병합 품질 체크리스트(계약 대조·스타일 일관성·스캐폴딩 제거·tester 커버리지 대조) 수행. 동작이 다른 중복 로직·파일 소유권 위반은 임의 처리하지 않고 PM에게 보고. 구현 자체가 없는 항목(coverage gap)은 PM에게만 보고하고 agent-builder를 직접 호출하지 않음 | Read, Edit, Write, Glob, Grep, Bash, Agent | sonnet (통합 판단·충돌 해결에 추론 필요) |
| tester | `.claude/agents/tester.md` | 통합된 코드에서 spec.md의 각 기능이 실제로 동작하는지 테스트 작성/실행, 기능별 pass/fail과 실패 원인 보고. 로직 버그를 직접 고치지 않음 (보고만) | Read, Write, Glob, Grep, Bash | sonnet (수용 기준에 맞는 테스트 설계에 추론 필요) |

## 루프/폭주 방지 정책

새 "검사관 에이전트"는 만들지 않는다 — 루프 감지·중단 권한을 또 다른 에이전트에게 주면 PM과
통제 축이 두 개로 쪼개져서, "팀 구성/실행 흐름 결정은 PM 한 곳"이라는 원칙이 깨진다. 대신 아래
규칙을 기존 에이전트 계약과 PM 자신에게 박아둔다.

- **재시도 상한 (3-strikes rule)**: team-lead ↔ tester 사이에서 같은 feature가 3회 연속
  FAIL/BLOCKED로 돌아오면, team-lead는 더 시도하지 않고 `blocked — escalated`로 표시해 PM에게
  즉시 보고한다 (`team-lead.md` 반영 완료).
- **도구 호출 재시도 상한**: 어떤 에이전트든 같은 도구 호출이 같은 이유로 2회 연속 실패하면,
  세 번째를 스스로 시도하지 말고 그 실패 내용을 그대로 보고한다 (`tester.md` 반영 완료; 다른
  에이전트를 새로 만들 때도 이 규칙을 기본으로 넣는다).
- **기계적 디스패치 로그 (`RUN_LOG.md`)**: PM과 team-lead는 에이전트를 부를 때마다 해당 로그
  (하네스 자체 변경이면 `<하네스 루트>/RUN_LOG.md`, 특정 프로젝트 작업이면 그 프로젝트 루트의
  `RUN_LOG.md` — 범위 구분은 바로 아래 "RUN_LOG.md 범위 규칙" 참고)에 한 줄을 남긴다 (호출자,
  대상, feature/작업명, 시도 횟수, 사유). 대화 컨텍스트가 요약되어도 "몇 번째 호출인지"를
  파일로 확인할 수 있게 하기 위함 — 루프 판정을 LLM의 기억이 아니라 기계적으로 셀 수 있는
  기록에 근거하게 한다. PM이 서브에이전트 없이 직접 작업(코드 직접 작성 등, 원래는 예외적
  상황)한 경우에도 최소 한 줄은 남긴다 — 안 부르면 로그도 안 남는 구조 자체가 이전에 실제
  공백을 만든 적이 있다(2026-09-14, hollinone 프로젝트 사례).

### 문서 네이밍·배치·작업트리 규칙 (2026-09-14 정리 — 이후 "에이전트만을 위한 범용 하네스" 원칙으로 확정)

이 하네스는 **특정 프로젝트에 종속되지 않는, 순수 에이전트/프로세스 인프라**다 — 프로젝트 코드나
프로젝트별 SPEC.md/RUN_LOG.md를 하네스 폴더 안에 두지 않는다. (계기: 여러 프로젝트에 재사용하며
하네스 자체 변경 이력과 프로젝트별 작업 이력이 `RUN_LOG.md` 한 파일에 뒤섞이는 문제를 겪었고,
`todo-cli/` 같은 실제 프로젝트 폴더까지 하네스 안에 들어와 있던 걸 발견 후 전부 제거함. `SPEC.md`가
`SPEC_LOG_PJT.md`로, `RUN_LOG.md`가 `RUN_LOG_ARCHIVE.md`로 변형되어 흩어졌던 것도 같은 계기로
발견·정리됨.)

**문서명 고정(변형 금지)**: 아래 표에 있는 문서는 항상 이 이름 그대로 쓴다 — 프로젝트명, 주제명,
버전 같은 걸 파일명에 붙이지 않는다(`SPEC_LOG_PJT.md`, `RUN_LOG_ARCHIVE.md`, `tech_spec.md` 같은
변형 전부 금지). 프로젝트를 구분하는 건 파일명이 아니라 **폴더 위치**로 한다 — 같은 이름의 파일이
여러 프로젝트 폴더에 각각 있는 건 정상이다.

| 문서명 | 누가 쓰는가 | 어디에 두는가 |
|---|---|---|
| `PRD.md` | spec-writer 전용 소유 (PRD 모드일 때만 생성 — 목표·성공기준·비목표·상위 기능목록. 착수 시점 1회성 스냅샷, `SPEC.md` 작성 후 재갱신 안 함) | 그 프로젝트 루트(하네스 폴더 바깥), `SPEC.md`와 같은 위치 |
| `SPEC.md` | spec-writer 전용 소유 (기능 명세서 — 기능별 요구사항·수용기준) | 그 프로젝트 루트(하네스 폴더 바깥) |
| `TECH_SPEC.md` | tech-spec-writer 전용 소유 (기술 설계 — `SPEC.md`가 있으면 그 내용을 참조·인용하되 파일은 분리) | 그 프로젝트 루트(하네스 폴더 바깥), `SPEC.md`와 같은 위치 |
| `RUN_LOG.md` | PM, team-lead | 하네스 루트(하네스 자체 변경용) **와** 각 프로젝트 루트(그 프로젝트 작업용) — 각각 별개 파일, 용도는 아래 참고 |
| `ORCHESTRATION.md`, `CODING_GUIDE.md`, `HANDOFF.md`, `CHANGELOG.md` | PM | 하네스 루트에만, 딱 1개씩만 존재 |

**`SPEC.md`/`TECH_SPEC.md`를 분리한 이유**: 처음엔 tech-spec-writer가 spec-writer의 `SPEC.md`를
"이어서 확장"하게 했었는데, 두 문서는 구조 자체가 다르다(tech-spec-writer만의 "기존 코드 구조
분석", "신규 구현 함수 명세" 섹션이 있음) — 한 파일에 합치면 확장이 아니라 사실상 재구조화가
되어 병합 실수 위험이 있고, "한 파일=한 소유자"라는 파일 소유권 원칙(아래 "구현 에이전트 디스패치
원칙" 참고)에도 어긋난다. 그래서 **이름은 고정하되 파일은 분리**하는 것으로 정정함(2026-09-14).

새 문서 타입이 필요해지면(예: 새 에이전트가 새로운 산출물을 만든다), 그 에이전트를 만들 때
PM이 표준명을 정해서 이 표에 추가한다 — 에이전트가 알아서 이름을 짓게 두지 않는다.

**배치 규칙**: 하네스 레벨 문서는 하네스 루트에만. 프로젝트 문서(`SPEC.md`, 프로젝트용
`RUN_LOG.md`)는 항상 **이 하네스 폴더 바깥**, 그 프로젝트 자신의 루트에 둔다.

- **`<하네스 루트>/RUN_LOG.md`**: 하네스 자체의 변경만 기록한다 — 에이전트 신설/수정
  (`.claude/agents/*.md`), 정책·규칙 변경(`ORCHESTRATION.md`/`CODING_GUIDE.md` 개정) 등.
- **프로젝트별 `RUN_LOG.md`/`SPEC.md`**: 항상 **이 하네스 폴더 바깥**, 그 프로젝트 자신의 루트에
  둔다. 그 프로젝트의 기능 구현을 위한 모든 에이전트 디스패치(구현/통합/테스트/PM 직접 작업)는
  거기에 기록하며, 하네스 쪽 `RUN_LOG.md`에는 적지 않는다.
- **판단 기준**: "이 로그가 하네스를 재사용하는 *다음* 프로젝트에도 의미가 있는가?" → 하네스
  `RUN_LOG.md`. "이 프로젝트에서만 의미 있는가?" → 그 프로젝트의 `RUN_LOG.md`.
- **새 프로젝트를 시작할 때**: PM은 그 프로젝트 루트(하네스 폴더 바깥)에 `RUN_LOG.md`·`SPEC.md`를
  만드는 것을 착수 체크리스트에 포함한다 — 하네스 폴더 안에는 절대 만들지 않는다.

**작업트리 확인 규칙**: PM은 파일을 만들거나 옮기거나 지우는 작업(문서 정리, 구조 변경 등) 뒤,
사용자에게 완료를 보고하기 전에 관련된 모든 git 저장소에서 `git status`로 작업트리를 확인한다 —
의도치 않은 변경, 지운 줄 알았는데 남은 파일, 새로 생겼는데 추적 안 되는 파일이 없는지 검증한다.
**특히 하네스 저장소의 `git status`에 프로젝트 코드나 `SPEC.md`/`TECH_SPEC.md`/프로젝트용
`RUN_LOG.md` 같은 프로젝트 문서가 나타나면, 그 자체가 위 "배치 규칙" 위반 신호다** — 즉시 해당
파일을 프로젝트 루트(하네스 폴더 바깥)로 옮기고 나서 보고한다.
`git add`는 항상 `git status`로 무엇이 바뀌었는지 먼저 본 뒤, 그 작업과 관련된 것만 골라서 한다
(`git add -A`를 습관적으로 먼저 쓰지 않는다) — 무관한 변경이 섞여 들어가는 걸 막기 위함이다.

- **PM 레벨 하드 상한과 에스컬레이션**: `RUN_LOG.md` 기준으로 같은 feature/에이전트에 대한 호출이
  N회(기본값 3~5회, 프로젝트 성격에 따라 조정)를 넘으면 PM은 더 진행하지 않고 사용자에게 상황을
  보고하고 판단을 요청한다 (AskUserQuestion). 서브에이전트는 사용자에게 직접 물어볼 도구가 없으므로,
  루프 탈출의 최종 책임은 구조적으로 PM에게 있다.
- **사후 분석이 필요하면 새 에이전트 대신 `doc-writer`를 재사용**한다 — `RUN_LOG.md`를 읽고 왜
  막혔는지 요약 문서를 쓰는 건 실행에 개입하지 않는 순수 문서화 작업이라 통제 축을 늘리지 않는다.

## 이상 감지 책임 분담

"문제를 누가 감지하는가"는 한 곳에 몰아주지 않는다. 각 계층은 **자기 눈에 보이는 범위의 문제만
감지**하고, 자기가 해결 못 하는 건 위로 올린다. 감지 권한을 한 명(예: tester 하나)에게 몰아주면
그 계층 시야 밖의 문제는 아무도 못 보게 된다.

| 계층 | 감지 대상 | 어떻게 아는가 | 보고 대상 |
|---|---|---|---|
| 구현 에이전트 | 자기 작업 중 만난 문제 — 요구사항 모호함, 빌드 실패, 의존성 문제, 자기 코드가 로컬에서부터 안 됨 | 작업하면서 스스로 부딪힘 | 호출한 쪽 (PM 또는 team-lead) |
| tester | 기능이 spec.md의 수용 기준대로 **실제로 동작하는가** | 독립적으로 테스트 실행 — 구현자의 "됐다"는 자기 보고를 신뢰하지 않고 직접 검증 | team-lead |
| team-lead | 통합 수준 문제 — 코드 합칠 때 충돌/배선 오류, 커버리지 갭, tester의 반복 실패 패턴(3-strikes) | 코드 통합 과정 + tester 보고 취합 | PM |
| PM | 프로젝트 전체/반복 문제 — RUN_LOG.md 기준 특정 대상이 계속 막힘, 팀 구성 자체가 잘못됨 | RUN_LOG.md 파일 기준 기계적 카운트 | 사용자 |

**핵심 원칙: 구현 에이전트의 자기 보고는 검증이 아니다.** "제가 만든 기능, 문제없어요"라는 구현
에이전트의 말은 tester의 독립적 검증을 대체하지 못한다 (team-lead가 스스로 완료 판단을 못
하는 것과 같은 이유 — 만든 사람이 자기 검사관을 겸하면 맹점이 생긴다). tester는 반드시 구현
에이전트의 보고와 무관하게 직접 테스트를 돌려서 확인한다.

## 구현 에이전트 디스패치 원칙 (파일 소유권)

**배경 (실제 발생한 사고):** todo-cli 시험 운행에서 implementer-A와 implementer-B가 같은 파일
(`taskStore.js`)에 동시에 write해서 경합이 발생했다. 이번엔 PM이 둘에게 준 인터페이스 계약이
글자 그대로 동일해서 우연히 무사했지만, 계약이 조금만 어긋났어도 한쪽이 다른 쪽을 조용히
덮어써서 발견 못 할 뻔했다 (`RUN_LOG.md`의 2026-09-11 관찰 기록 참고).

**그래서 PM이 구현 에이전트를 디스패치할 때 지키는 규칙:**

- **배타적 경로 지정**: 각 구현 에이전트에게 "네가 쓸 수 있는 파일/경로"를 명시적으로 지정한다.
  둘 이상이 같은 경로에 쓰도록 디스패치하지 않는다.
- **스크래치 디렉터리 격리 (기술적 강제)**: 프롬프트로 "네 경로만 써라"라고 부탁하는 것만으론
  실시간 차단이 안 되므로, 실제로는 **각 구현 에이전트에게 별도의 스크래치 디렉터리**를 주고
  (예: `<프로젝트>/.scratch/<구현자 이름>/`), 최종 공유 경로가 아니라 그 안에서 작업하게 한다.
  최종 파일 구조와 동일한 상대 경로로 스크래치 디렉터리 안에 쓰게 하므로, 나중에 team-lead가
  그대로 옮기기만 하면 된다. 이러면 최종 공유 경로에는 애초에 아무도 동시에 못 쓰니, 경합이
  물리적으로 불가능해진다 (git worktree 없이도 얻는 격리 효과).
- **스텁도 자기 스크래치 안에서는 자유롭게**: 아직 존재하지 않는 의존성(다른 구현자가 병렬로
  만들고 있는 파일)을 로컬 테스트용으로 흉내 내야 한다면, 자기 스크래치 디렉터리 안의 원래
  상대 경로에 그대로 스텁을 써도 된다 — 어차피 자기 스크래치 공간이라 최종 공유 경로를 건드릴
  위험이 없다 (이전엔 스텁 파일명을 따로 바꾸라고 했지만, 스크래치 격리가 이를 대체한다).
- **통합 시 조립(assembly)은 team-lead 몫**: team-lead는 통합 시작 전에, 소유권 맵을 기준으로
  각 파일을 담당 구현자의 스크래치 디렉터리에서 최종 경로로 복사해 조립한다. 같은 파일명이
  여러 스크래치 디렉터리에 있어도, 소유권 맵상 소유자가 명확하면 그 버전만 채택하고 나머지는
  (남의 스텁이었을 뿐이므로) 조용히 버린다 — 문제로 보고하지 않는다. 다만 소유자가 불분명하거나
  겹치는 경우는 team-lead가 임의로 고르지 않고 PM에게 보고한다 (`team-lead.md` 반영 완료).
- **`CODING_GUIDE.md`도 항상 같이 전달**: 최소 수정 원칙·네이밍·주석·에러 처리·로깅 규칙을 담은
  `CODING_GUIDE.md`(신규, 프로젝트 루트)를 PM이 구현 에이전트 디스패치 시 SPEC.md·인터페이스
  계약과 함께 항상 같이 전달한다. team-lead는 병합 품질 체크리스트에서, tester는 엣지/예외/로깅
  검증에서 이 문서를 기준으로 삼는다 (`team-lead.md`, `tester.md` 반영 완료). 이건 "에이전트가
  어떻게 행동할지"가 아니라 "만들어지는 코드가 어떤 모양이어야 하는지"에 대한 규칙이라
  `ORCHESTRATION.md`와는 별도 문서로 분리했다.
- **인터페이스 계약은 PM이 사전에 명문화**: 파일을 나누는 것만으로는 부족하다 — 함수 시그니처,
  필드명 등 공유 인터페이스를 PM이 각 구현 에이전트의 디스패치 프롬프트에 동일하게 못 박아줘야
  결과가 호환된다. 스크래치 격리를 쓰면 구현자들이 서로의 실제 코드를 실시간으로 못 보므로,
  이 사전 계약의 정확도가 더 중요해진다 (계약이 어긋나면 조립 시점에야 드러남 — 격리의 역설적
  단점, 아래 참고).
- **작업 완료 시 파일 단위 변경 요약 필수**: 모든 구현 에이전트는 작업을 마치면 어떤 파일을
  만들었는지/고쳤는지, 각 파일에서 무엇을 바꿨는지, 의도적으로 건드리지 않은 부분은 무엇인지를
  짧게 요약해 호출한 쪽(PM 또는 team-lead)에게 보고한다. team-lead도 통합 완료 보고 시 이 요약을
  취합해 PM에게 전달한다. (병합 품질 체크리스트의 "계약 리터럴 대조"가 이 요약을 근거로 빠르게
  이뤄질 수 있게 하기 위함 — 요약 없이 diff를 처음부터 다 읽어야 한다면 체크리스트 자체가
  느려진다.)
- **역설적 단점 (알고 감수하는 트레이드오프)**: 격리 안 했을 땐 경합이 나더라도 비교적 바로
  드러나지만, 스크래치로 격리하면 계약이 미묘하게 어긋나도 각자 쪽에서는 다 잘 도는 것처럼
  보이다가 team-lead의 조립·통합 단계에 가서야 드러날 수 있다. 그래서 사전 계약 정확도가 더욱
  중요해진다.
- **버전 관리(git)로 롤백 지점 확보 — 이제 기술적으로 강제됨**: 이 하네스는 그 자체로 git
  저장소다(어느 프로젝트 루트에 복사해 넣든 유지). `.claude/settings.json`의 permissions에 `Bash(git commit:*)`, `Bash(git tag:*)`,
  `Bash(git push:*)`, `Bash(git reset:*)`를 **"ask"(항상 승인 필요)**로 등록해뒀다 — 이건
  team-lead가 프롬프트를 무시해도 실제로 막히는 하드 게이트다. 동시에 이게 "PM도 임의로 커밋하지
  않고 사용자와 상의한다"는 규칙의 기술적 구현이기도 하다 — 승인 요청 자체가 곧 상의 절차다.
- **체크포인트/의미있는 커밋 이원화**: `scripts/checkpoint-commit.sh`는 permissions의 "allow"에
  등록해 승인 없이 자동 실행되도록 했다. team-lead는 통합·검증 직후 이 스크립트로 **체크포인트
  커밋만** 자동으로 남기고 (`team-lead.md` 8단계), 실제 `git commit`/`tag`/`push`는 시도조차
  하지 않는다. "의미있는" 커밋/태그(기능 완성, 정책 변경 등)는 여전히 PM이 사용자와 상의한 뒤
  직접 `git commit`을 실행하며, 이때도 permissions의 ask 규칙 때문에 실제 승인 단계를 거친다.
- **왜 이렇게 나눴나**: 순수 "매번 상의"만으로는 team-lead의 통합 직후 롤백 지점이 한동안
  비어있는 공백이 생긴다 (사용자 확인을 기다리는 동안 안전망이 없음). 체크포인트는 즉시 자동으로
  안전망을 만들고, 의미있는 커밋만 상의 대상으로 좁혀서 안전성과 사용자 통제권을 동시에 만족시킨다
  (2026-09-11, 사용자와 논의 후 확정).
- **`npm install` 등 외부 의존성 설치도 같은 방식으로 ask 등록**: `CODING_GUIDE.md`의 "외부
  npm 의존성 금지" 규칙도 프롬프트뿐 아니라 permissions로 이제 실제 승인 게이트가 걸려 있다.
- **버전 태깅**: 의미 있는 단위(한 사이클 완료, 정책 보강 등)마다 PM이 `CHANGELOG.md`에 항목을
  추가하고 git 태그(`v0.MINOR.PATCH`, semver의 0.y.z 관례)를 붙인다 — 이때도 마찬가지로 사용자와
  상의 후 진행한다. 태그를 올리는 기준은 `CHANGELOG.md` 상단에 명시되어 있다 — 새 에이전트/구조
  변경은 MINOR, 기존 규칙 보강은 PATCH.

## 병합 품질 체크리스트 (team-lead)

"통합해라"만으로는 부족해서, team-lead가 통합 완료를 선언하기 전에 반드시 확인하는 4가지를
`team-lead.md`에 명시해두었다:

1. **계약 리터럴 대조** — tester PASS와 별개로, PM이 준 인터페이스 계약(시그니처·필드명)과
   실제 코드를 글자 그대로 대조.
2. **스타일/네이밍 일관성** — 서로 다른 구현자의 코드 사이 네이밍·에러 메시지 톤 불일치를 통합
   시 정리.
3. **스캐폴딩 잔재 제거** — 구현자 각자의 로컬 테스트용 스텁/임시 코드가 최종 통합본에 남지
   않았는지 확인.
4. **tester 커버리지 대조** — tester의 PASS가 spec의 수용 기준 항목을 빠짐없이 짚었는지 확인.
   빠진 게 있으면 tester를 다시 불러 보완시킨다 (실패 재시도가 아니라 커버리지 보완이므로
   3-strikes 카운트에는 포함하지 않음).

또한, **동작이 다른 중복 로직**(예: 두 구현자가 같은 규칙을 서로 다르게 구현)을 발견하면
team-lead가 임의로 하나를 골라 병합하지 않고 PM에게 보고하도록 명시했다 — 이건 스타일 정리가
아니라 정답이 뭔지 판단해야 하는 스펙 문제라서, team-lead 혼자 결정할 사안이 아니다.

## 알려진 한계

- ~~team-lead → tester 위임(Agent 도구)이 실제로 이 harness에서 지원되는지 미검증~~ →
  **확인됨.** todo-cli 시험 운행에서 `subagent_type: "general-purpose"` + "tester.md 정의를
  그대로 따르라"는 프롬프트 우회 방식으로 실제 위임이 성공했다 (RUN_LOG.md 2026-09-11 14:50~15:05
  참고). 이 harness는 이름 있는 커스텀 서브에이전트 타입을 네이티브로 인식하지 못하므로, 앞으로도
  이 우회 방식(general-purpose + 역할 정의 파일 지정)을 표준 호출법으로 쓴다.
- **"이 파일은 건드리지 마라" 류 경계는 기술적 강제가 아니라 프롬프트상의 소프트 제약.** 예: team-lead의 Write/Edit, tester의 Write는 도구 자체가 특정 파일/범위로 제한되지 않으므로, 실제로는 시스템 프롬프트의 "Does NOT" 문구에 의존함. 완전한 강제가 필요하면 별도 파일 권한 체계가 있어야 함. (파일 소유권 원칙도 마찬가지로 소프트 제약 — team-lead의 사후 감지에 의존한다.)

## 새 에이전트를 추가하는 절차

새 에이전트가 필요하다는 신호는 두 군데서 올 수 있다 — PM이 스펙을 검토하다가 직접 판단하거나,
team-lead가 통합 중 coverage gap을 보고하는 경우. **어느 쪽이든 실제로 `agent-builder`를 호출하는
주체는 항상 PM이다.** team-lead·tester·spec-writer 등 다른 에이전트는 신규 에이전트 필요성을
PM에게 "요청/보고"만 할 수 있고, 직접 만들거나 서로 요청을 주고받지 않는다 (등록표가 PM 한 곳에서만
갱신되도록 하기 위함).

1. 필요 신호 접수 — PM이 스펙을 읽다가 판단하거나, team-lead의 coverage gap 보고를 받는다.
2. PM이 그 신호를 다음 중 하나로 분류한다: (a) 기존 에이전트를 다른 입력으로 재호출하면 되는 경우 → 그냥 재호출, (b) 정말 새로운 책임·권한 조합이 필요한 경우 → 3단계로.
3. PM이 필요한 역할을 한 문장으로 정의한다 (책임, 호출 시점, 알려진 제약).
4. `agent-builder`를 호출해 위 스펙을 전달한다.
5. `agent-builder`가 `.claude/agents/<이름>.md`를 작성하고, 부여한 권한과 그 이유를 3줄 이내로 보고한다.
6. PM이 이 표에 새 행을 추가해 기록을 갱신한다 (필요하면 `doc-writer`에게 표 정리를 맡길 수 있다).

## 원칙 (합의된 내용 요약)

- 권한은 "책임 범위에 필요한 최소치"로 시작하고, 필요할 때만 넓힌다.
- 원본 데이터(코드, 로그, 검색 결과)는 전문 에이전트가 소비하고, PM은 압축된 결론만 받는다.
- 조사(Explore류)와 문서화(doc-writer류)는 분리한다 — 문서화 에이전트가 스스로 조사를 시작하는 순간 조사 에이전트와 다를 바 없이 무거워진다.
- 포맷팅/템플릿 작업은 저비용 모델(haiku)로, 판단이 필요한 작업은 상위 모델로 배정한다.
- **팀 구성(어떤 에이전트가 존재하는지) 변경 권한은 PM에게만 있다.** 다른 에이전트끼리 직접 통신해서
  새 에이전트를 만들거나 요청을 주고받지 않는다 — 그 순간 등록표가 여러 곳에서 갱신되며 깨지고,
  권한 승인(least privilege 검토)을 거치지 않은 에이전트가 생길 위험이 커진다.
- **확신 없는 부분은 확신 없다고 명시한다.** 요구사항이 애매하거나 실행 없이는 확인 불가능한
  사실은, 아는 척 추측해서 진행하지 않고 "미확정/실측 필요"로 표시해 위 계층에 넘긴다
  (spec-writer의 "미확정 사항" 관행을 전 에이전트 공통 원칙으로 일반화).
