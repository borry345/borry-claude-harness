# 세션 인수인계 (Handoff)

새 세션을 열었다면 **이 문서를 먼저 읽는다.** 배경 지식(팀 구조, 정책, 코딩 규칙)은 아래 문서에
이미 다 있으니 여기서 중복하지 않는다:

1. `ORCHESTRATION.md` — 항상 읽는다 (팀 구조, 권한 경계, 정책).
2. `CODING_GUIDE.md` — 항상 읽는다 (코드 스타일 규칙).
3. **이 하네스는 프로젝트 콘텐츠(코드, 프로젝트별 SPEC.md/RUN_LOG.md)를 담지 않는다**(2026-09-14
   확정 — "에이전트만을 위한 범용 하네스"로 정리, 이전엔 `todo-cli/`처럼 실제 프로젝트 폴더가
   하네스 안에 같이 있었음). 실제 작업은 이 하네스를 프로젝트 루트에 복사해 넣고, 그 프로젝트
   루트에 자체 `SPEC.md`/`RUN_LOG.md`를 두는 식으로 진행한다 — 그 프로젝트의 진행 상황은
   그 프로젝트 쪽에서 확인한다.

이 문서 자체는 **위 문서들에 없는, 세션이 바뀌면 끊기는 하네스 레벨 정보만** 담는다.

## 현재 하네스 레벨 상태 (2026-09-14)

- 이 하네스는 **범용/추상 — 특정 프로젝트에 종속되지 않음**이 설계 원칙으로 확정됨. 최근 정리로
  프로젝트 인스턴스(예: `todo-cli/`, 로그 관리 PJT 아카이브)를 전부 제거했고, `ppjt2` 같은 특정
  프로젝트명에 묶여 있던 문구도 전부 일반화함.
- 그 밖의 최근 변경: `tech-spec-writer` 에이전트 신설(코드/레퍼런스를 직접 읽는 기술 스펙 작성자
  — 기존 `spec-writer`는 코드 미열람 원칙이라 이 역할을 못함), `tester.md`에 회귀 테스트 우선
  규칙 추가, `RUN_LOG.md`를 "하네스 자체 변경만" 기록하는 용도로 재정의.
- 하네스 레벨에서 현재 진행 중인 별도 작업은 없음. 다음 세션은 바로 새 요청을 받으면 된다.

## 잊으면 안 되는 것 (ORCHESTRATION.md에 이미 있지만 특히 놓치기 쉬운 것만 재강조)

- **네이티브 서브에이전트 타입 미지원**: `.claude/agents/*.md`의 커스텀 이름(`spec-writer`,
  `team-lead` 등)은 Agent 도구의 네이티브 `subagent_type`으로 뜨지 않는다(뜨는 건 `claude`,
  `claude-code-guide`, `Explore`, `general-purpose`, `Plan`, `statusline-setup`뿐 — 환경마다
  다를 수 있으니 실제 목록을 확인할 것). 항상 `subagent_type: "general-purpose"` +
  "`.claude/agents/<이름>.md` 정의를 그대로 따르라"는 프롬프트로 우회한다
  (`ORCHESTRATION.md`의 "알려진 한계" 참고).
- **git commit/tag/push는 항상 사용자와 상의**(체크포인트 커밋 스크립트 예외) — 상세 근거는
  `ORCHESTRATION.md`의 "구현 에이전트 디스패치 원칙" 참고.
- **프로젝트 폴더를 하네스 안에 다시 만들지 않는다** — 실제 코드/프로젝트별 스펙은 항상 이
  하네스 폴더 바깥(프로젝트 루트)에 둔다. 이걸 어기면 다음에 이 하네스를 다른 프로젝트로 복사해
  쓸 때 또 정리해야 한다(2026-09-14, 실제로 한 번 발생했던 문제).
