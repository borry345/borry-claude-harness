# SPEC_LOG_PJT — 인포테인먼트 Setting 연동 + 로그 데이터 관리 웹페이지

## 1. 개요 (Overview)

기존 차량용 태블릿 인포테인먼트 SPA(`pjt2/1649641/`, 다크테마 `#121214`, `switchView()` 기반 단일 페이지 뷰 전환)에 현재 존재하지 않는 "Setting" 진입 버튼을 Footer에 추가한다. 이 버튼은 형제 폴더로 신설되는 별도 페이지 애플리케이션 `pjt2/1649641-pjt2/`(로그 데이터 관리 웹)로 사용자를 이동(멀티 페이지 내비게이션, 기존 SPA의 view-pane 전환 방식이 아님)시킨다. 로그 관리 앱은 다시 인포테인먼트 앱으로 돌아갈 수 있는 홈 복귀 버튼을 자체 Footer에 고정 노출해야 하며, 이로써 두 앱은 양방향으로 연동된다.

로그 관리 앱은 Firebase Firestore(`pjt2-c3b41` 프로젝트) + Firebase Functions를 사용해 다음 파이프라인을 구현한다: 사용자가 원시 로그 텍스트 파일을 업로드하면 행 단위로 분리되어 `raw_telematics_logs` 컬렉션에 저장되고(F201), Functions의 `onDocumentCreated` 트리거가 이를 감지해 정규식 기반으로 정제·구조화한 뒤(F202) `telematics_logs` 컬렉션에 표준화된 필드(시간/사용자ID/로그레벨/메시지 등, 로그마다 부가 필드가 다른 가변 스키마)로 저장한다(F203). 화면은 `onSnapshot` 실시간 구독으로 새로고침 없이 최신 로그를 상단에 표시하고(F204), 날짜 범위·로그 레벨·키워드 조합 필터링을 제공하며(F205), 로그 레벨(관측된 값: INFO/WARNING/CRITICAL, 그리고 예기치 못한 값에 대한 기본 처리 포함)에 따라 텍스트/배경 색상을 다르게 표시한다(F206). 심화 기능으로 특정 사용자 ID 기준 로그 선별 기능(F207)을 포함한다.

Firebase Functions의 실제 클라우드 배포(`firebase deploy --only functions`)는 본 스펙의 범위 밖이며, 정제 로직이 로컬에서 올바르게 동작함(단위 테스트로 검증 가능)까지만 수용 기준에 포함한다.

## 2. 기능 목록 (Feature List)

| ID | 기능명 | 구분 |
|----|--------|------|
| F200 | 인포테인먼트 Setting 진입 버튼 및 양방향 내비게이션 | 신규(연동) |
| F201 | 원시 로그 데이터 수집 | 필수 |
| F202 | 실시간 데이터 정제 처리 | 필수 |
| F203 | 구조화된 로그 데이터 저장 | 필수 |
| F204 | 로그 실시간 시각화 | 필수 |
| F205 | 데이터 상세 필터링 및 검색 | 필수 |
| F206 | 로그 등급별 시각적 차별화 | 필수 |
| F207 | 사용자 맞춤형 로그 분석 | 심화(선택) |

## 3. 기능별 요구사항 및 수용 기준 (Functional Requirements & Acceptance Criteria)

### F200 — 인포테인먼트 Setting 진입 버튼 및 양방향 내비게이션

**요구사항**: 기존 인포테인먼트(`1649641`) Footer에 기존 3개 버튼(Home/Navi/Weather)과 동일한 시각 스타일의 Setting 버튼을 추가한다. Setting 버튼 클릭 시 `switchView()`(뷰 전환)가 아니라 실제 페이지 이동으로 로그 관리 앱(`1649641-pjt2`)의 진입 페이지로 이동한다. 로그 관리 앱은 공식 레이아웃 ⑥ Footer 요구사항(홈 복귀 버튼 + 주요 메뉴 바로가기, 고정 노출)에 따라 인포테인먼트로 돌아가는 버튼을 항상 노출한다. 로그 관리 앱의 시각 스타일은 인포테인먼트와 톤(다크테마, `#121214` 배경, 터치 친화적 버튼 크기)을 맞춘다.

**수용 기준**:
- 인포테인먼트 `index.html`을 로드했을 때 Footer(`.footer-nav-buttons`) 안에 기존 Home/Navi/Weather 버튼과 함께 Setting 버튼이 정확히 1개 존재해야 한다.
- Setting 버튼은 기존 3개 버튼과 동일한 클래스 체계(`touch-target footer-btn` 상당의 터치 타깃 크기)를 가져야 하며, 클릭 가능 영역이 다른 Footer 버튼과 시각적으로 동일한 크기여야 한다.
- Setting 버튼을 클릭하면 브라우저가 `1649641-pjt2`의 진입 페이지로 실제 이동(URL/문서 전환)해야 한다 — 기존 3개 버튼처럼 같은 `index.html` 내 view-pane이 보이고 숨겨지는 방식이어서는 안 된다.
- 로그 관리 앱 진입 페이지를 로드했을 때, 뷰포트를 스크롤하지 않아도 하단에 고정된 Footer 영역이 보여야 하고, 그 안에 홈 복귀 버튼이 항상 존재해야 한다.
- 로그 관리 앱의 홈 복귀 버튼을 클릭하면 인포테인먼트 앱의 진입 페이지(`1649641/index.html`)로 실제 이동해야 한다.
- 로그 관리 앱의 모든 페이지에서 기본 배경색이 인포테인먼트와 동일한 `#121214` 계열의 다크 테마여야 하고(개발자 도구 등으로 계산된 배경색 확인 가능), 버튼 요소는 태블릿 터치 조작에 적합한 크기(기존 인포테인먼트 Footer 버튼과 유사한 크기)를 가져야 한다.
- Setting 버튼 도입 후에도 기존 Home/Navi/Weather 버튼의 `switchView()` 동작(기존 SPA 내 뷰 전환)은 그대로 동작해야 한다(회귀 없음).

### F201 — 원시 로그 데이터 수집

**요구사항**: 사용자가 로그 텍스트 파일을 업로드하면, 파일 내용을 행 단위로 분리하여 각 행을 `raw_telematics_logs` 컬렉션에 개별 문서로 저장한다. 각 문서는 최소한 원본 텍스트 줄을 담은 필드(레퍼런스 기준 `{ log: <line> }` 형태)를 포함한다.

**수용 기준**:
- 사용자가 파일 선택 UI를 통해 텍스트 파일(예: 샘플과 동일한 형식의 `.txt`)을 선택하고 업로드를 실행하면, 파일의 줄 수만큼 `raw_telematics_logs`에 새 문서가 생성되어야 한다(빈 줄 처리 정책은 §4 미확정 사항 참조).
- 각 생성된 문서는 해당 줄의 원본 텍스트를 그대로 보존한 필드를 가져야 한다 — 파싱이나 가공 없이 원문 그대로 저장한다(정제는 F202의 책임이며 F201에서는 발생하지 않아야 한다).
- 업로드 완료 후 사용자에게 업로드가 끝났음을 알 수 있는 피드백(성공/완료 표시)이 있어야 한다.
- 1000줄 규모(제공된 샘플 `vehicle_logs_1000_with_missing.txt` 기준)의 파일을 업로드해도 오류 없이 전체 줄이 처리되어야 한다.
- 파일을 선택하지 않고 업로드를 시도한 경우, Firestore에 아무 문서도 생성되지 않고 사용자에게 안내(에러 메시지 등)가 표시되어야 한다.
- 업로드된 각 줄의 순서와 `raw_telematics_logs`에 문서가 생성되는 순서(또는 이후 F202/F203에서 활용 가능한 순번 정보) 간의 관계가, 최소한 "같은 파일 내에서 나중 줄이 먼저 줄보다 먼저 처리된 것처럼 보이지 않아야 한다"는 수준까지는 보장되어야 한다(F204 정렬 기준과 연계).

### F202 — 실시간 데이터 정제 처리

**요구사항**: Firebase Functions v2의 `onDocumentCreated("raw_telematics_logs/{docId}")` 트리거가 `raw_telematics_logs`에 새 문서가 생성될 때마다 자동 실행되어, 원시 로그 줄을 정규식(`^\[(.*?) \| (.*?) \| (.*?) \| (.*)` 패턴)으로 `timestamp`/`user`/`log_level`/`message` 4개 필드로 분해한다. 이어서 `message`를 공백 기준으로 분리하고 `key=value` 형태 토큰에서 값이 "음수가 아닌 숫자"인 항목만 추가 필드로 채택한다(값이 비어있거나 숫자가 아니거나 음수인 항목은 결과 문서에서 아예 필드로 생성되지 않는다). `timestamp` 문자열은 `Date` 객체로 변환을 시도한다.

**수용 기준**:
- 예시 원시 로그 `[2025-07-06 09:57:00 | user_ssafy | INFO | Speed= km/h  Accel=-2.7 m/s²  Brake=ON  Dist=184.3 km  EngTemp=77.9 °C  FuelEff=19.2 km/L`가 주어졌을 때, 정제 결과 문서는 `user` = `user_ssafy`, `log_level` = `INFO`, `timestamp`가 해당 시각을 나타내는 값으로 설정되어야 한다.
- 위 예시에서 `Speed=` 는 값이 비어 있으므로 결과 문서에 `Speed` 필드가 생성되지 않아야 한다.
- 위 예시에서 `Accel=-2.7`은 음수이므로 결과 문서에 `Accel` 필드가 생성되지 않아야 한다.
- 위 예시에서 `Dist=184.3`, `EngTemp=77.9`, `FuelEff=19.2`처럼 값이 비어있지 않고 숫자이며 음수가 아닌 항목은 결과 문서에 해당 키의 숫자 필드로 포함되어야 한다.
- `Brake=ON`처럼 값이 숫자가 아닌 항목은 결과 문서에 해당 필드로 포함되지 않아야 한다.
- `raw_telematics_logs`에 문서가 1건 생성되면, 정제 로직이 정확히 1번 실행되어 `telematics_logs`에 그에 대응하는 문서가 1건 생성되어야 한다(중복 생성되지 않아야 한다).
- 이 정제 로직은 Firebase Functions 배포 없이 로컬 단위 테스트(입력 원시 로그 줄 → 기대 출력 필드 집합)로 검증 가능해야 한다 — 즉 정제 함수 본체가 Firestore 트리거 등록 코드와 분리되어, 순수 함수 형태로 테스트 가능해야 한다.
- 정규식 패턴에 매칭되지 않는 원시 로그 줄(형식이 깨진 줄, 헤더성 줄, 공백만 있는 줄 등)이 입력되었을 때의 동작은 §4 미확정 사항 참조.

### F203 — 구조화된 로그 데이터 저장

**요구사항**: F202에서 정제된 데이터를 `telematics_logs` 컬렉션에 규격화된 형태로 저장한다. 모든 문서는 공통 필드(시간/사용자ID/로그레벨 구분)를 갖되, 로그 메시지에서 파싱되는 부가 필드(Speed, Accel, Brake, Dist, EngTemp, FuelEff, Event, Impact, GForce, Location 등)는 로그마다 존재 여부가 다른 가변 스키마다.

**수용 기준**:
- `telematics_logs`의 모든 문서는 최소한 `timestamp`, `user`, `log_level`, `message`(또는 이에 상응하는 원본 메시지 텍스트) 4개 필드를 항상 포함해야 한다 — 이 중 어느 것도 빠져서는 안 된다(단, `timestamp` 파싱 실패 시 값 형태는 §4 참조).
- 메시지에서 유효한 숫자 부가 필드가 하나도 추출되지 않는 경우(예: 모든 key=value가 비어있거나 음수이거나 비숫자인 경우)에도, 문서는 정상 생성되어야 하며 4개 공통 필드만 가진 상태로 저장되어야 한다 — 부가 필드가 없다고 해서 문서 생성이 실패하거나 `null`/`undefined` 값의 필드가 채워져서는 안 된다(필드 자체가 존재하지 않아야 한다).
- 서로 다른 두 로그 문서가 서로 다른 부가 필드 집합(예: 하나는 `Speed`/`Accel`을 갖고 다른 하나는 `Event`/`Impact`를 가짐)을 가져도, 두 문서 모두 유효한 `telematics_logs` 문서로 조회 가능해야 한다(가변 스키마 허용).
- `log_level` 필드는 원시 로그의 레벨 문자열(`INFO`/`WARNING`/`CRITICAL` 등 관측된 값 포함, 그 외 값도 그대로)을 가공 없이 보존해야 한다.

### F204 — 로그 실시간 시각화

**요구사항**: `telematics_logs` 컬렉션을 Firestore `onSnapshot`으로 구독하여, 화면 새로고침 없이 새 로그가 실시간으로 반영되어야 한다. 최신 데이터가 목록 상단에 노출되어야 한다.

**수용 기준**:
- 로그 관리 앱의 시각화 화면을 열어둔 상태에서 새로운 원시 로그가 업로드되어 정제·저장이 완료되면, 페이지를 새로고침하지 않아도 해당 로그가 화면 목록에 자동으로 나타나야 한다.
- 새로 추가된 로그는 기존에 표시되던 로그들보다 화면 상단(목록의 맨 위)에 나타나야 한다.
- `telematics_logs` 컬렉션에 문서가 하나도 없는 최초 상태(빈 컬렉션)에서 화면을 열었을 때, 에러나 빈 화면 크래시 없이 "표시할 로그가 없음"을 알리는 안내가 보여야 한다.
- 이후 첫 로그가 추가되면 위 안내가 사라지고 목록에 해당 로그 1건이 나타나야 한다.
- 목록의 각 항목은 최소한 시간, 사용자, 로그 레벨, 메시지(또는 원문)를 화면에서 식별 가능해야 한다.
- 정렬 기준(어떤 필드로 "최신"을 판단하는지)은 §4 미확정 사항 참조.

### F205 — 데이터 상세 필터링 및 검색

**요구사항**: 날짜 범위(시작~종료), 로그 레벨, 메시지 키워드를 조합하여 로그를 필터링할 수 있어야 한다.

**수용 기준**:
- 시작 날짜와 종료 날짜를 지정하면, 지정 범위 내 `timestamp`를 가진 로그만 목록에 표시되고 범위 밖 로그는 제외되어야 한다.
- 로그 레벨을 선택하면, 선택된 레벨과 일치하는 `log_level`을 가진 로그만 표시되어야 한다.
- 메시지 키워드를 입력하면, 해당 키워드를 메시지(원문 텍스트)에 포함하는 로그만 표시되어야 한다.
- 날짜 범위 + 로그 레벨 + 키워드를 동시에 지정하면, 세 조건을 모두 만족하는 로그만 표시되어야 한다(AND 조합) — 어느 한 조건만 만족하는 로그는 제외되어야 한다.
- 필터를 적용했을 때 조건에 맞는 로그가 하나도 없으면, 에러 없이 "조건에 맞는 로그가 없음"을 알리는 빈 상태 안내가 표시되어야 한다.
- 필터를 모두 초기화(해제)하면 전체 로그 목록이 다시 표시되어야 한다.
- 필터가 적용된 상태에서도 F204의 실시간 반영(새 로그 자동 표시)은 계속 동작해야 한다 — 단, 새로 추가된 로그가 현재 필터 조건에 맞지 않으면 목록에 나타나지 않아야 한다.
- 날짜 범위 필터가 `timestamp` 파싱에 실패한(비정상적인) 로그에 대해 어떻게 동작해야 하는지는 §4 미확정 사항(F204와 동일 이슈) 참조.

### F206 — 로그 등급별 시각적 차별화

**요구사항**: 로그 중요도 레벨(`log_level`)에 따라 텍스트/배경 색상을 다르게 적용한다. 실제 데이터에서 관측되는 레벨은 `INFO`, `WARNING`, `CRITICAL` 세 가지이며, 이 세 가지 외의 예기치 못한 레벨 값(또는 값 없음)에 대한 기본(디폴트) 스타일도 포함해야 한다.

**수용 기준**:
- `log_level`이 `INFO`인 로그, `WARNING`인 로그, `CRITICAL`인 로그는 각각 서로 다른 텍스트 색상 및/또는 배경 색상 조합으로 표시되어야 하며, 세 조합은 육안으로 서로 구분 가능해야 한다.
- `log_level`이 위 세 값 중 어느 것도 아닌 경우(예: 알 수 없는 문자열, 또는 값이 비어있는 경우)에도 화면이 깨지거나 스타일이 적용되지 않은 채(예: 기본 검정 텍스트만) 방치되지 않아야 한다 — 정의된 기본(디폴트) 스타일이 적용되어야 한다.
- 정확한 색상 값(RGB/색상명)은 §4 미확정 사항 참조 — 본 수용 기준은 "레벨별로 시각적으로 구분되는 스타일이 존재하고, 알 수 없는 레벨에도 크래시 없이 기본 스타일이 적용된다"는 동작 자체를 검증 대상으로 한다.
- CRITICAL 등급은 다른 두 등급보다 시각적으로 더 눈에 띄는(예: 더 강한 대비/경고성 색상) 처리가 되어야 한다 — 중요도가 가장 높은 등급이 가장 낮은 시인성으로 표시되어서는 안 된다.

### F207 — 사용자 맞춤형 로그 분석 (심화/선택)

**요구사항**: 특정 사용자 ID(`log.user`, 즉 `telematics_logs`의 `user` 필드)를 기준으로 로그 데이터를 선별해서 볼 수 있어야 한다.

**수용 기준**:
- 특정 사용자 ID를 지정하면, 해당 `user` 필드 값과 일치하는 로그만 표시되어야 한다.
- 지정한 사용자 ID에 해당하는 로그가 하나도 없으면, 에러 없이 빈 상태 안내가 표시되어야 한다.
- 사용자 ID 지정을 해제하면 전체(또는 F205 필터 적용 상태의) 로그 목록으로 돌아와야 한다.
- F205의 필터(날짜/레벨/키워드)와 F207의 사용자 ID 필터를 함께 적용했을 때 모든 조건이 AND로 결합되어야 한다.
- F207이 F205와 별도 UI인지 통합 UI인지는 §4 미확정 사항 참조.

## 4. 미확정 사항 (Open Questions)

아래 항목은 PM이 사전 조사로 전달한 내용에도 명시되어 있지 않아, 임의로 가정하지 않고 PM/사용자 확인이 필요한 사항으로 남긴다.

1. **F201 빈 줄 처리**: 업로드한 파일에 빈 줄(공백만 있는 줄 포함)이 있을 때, 이를 그대로 `raw_telematics_logs`에 별도 문서로 저장해야 하는가, 아니면 업로드 단계에서 걸러야 하는가?
2. **F201 업로드 UI의 화면상 위치**: 공식 레이아웃(① 배경화면 ② Header ③ 설정·메뉴 사이드바 ④ 필터 입력 영역 ⑤ 시각화 메인 영역 ⑥ Footer)의 6개 영역 중 어디에 "파일 업로드" 진입점을 두어야 하는지 명시되어 있지 않다(예: 사이드바 내 버튼, 별도 업로드 전용 페이지, 시각화 화면 상단 등). 로그 관리 앱이 멀티 페이지라는 점은 확정되었으나, 업로드와 시각화가 같은 페이지인지 다른 페이지인지도 함께 확인이 필요하다.
3. **F202 정규식 비매칭 줄 처리**: 원시 로그 줄이 `^\[(.*?) \| (.*?) \| (.*?) \| (.*)` 패턴에 매칭되지 않을 때(형식이 깨진 줄, 헤더성 텍스트, 빈 줄 등) 정제 함수가 어떻게 동작해야 하는가 — 해당 원시 문서를 건너뛰는지, 에러 상태 문서를 `telematics_logs`에 남기는지, 아니면 함수 실행 자체가 실패해도 되는지가 명시되어 있지 않다.
4. **F202 timestamp 파싱 실패 시 처리**: `new Date()` 변환이 실패(유효하지 않은 날짜 문자열)했을 때 `telematics_logs` 문서의 `timestamp` 필드에 무엇이 저장되어야 하는가(원본 문자열 그대로 보존/`null`/필드 생략 등)? 이는 F204의 정렬 기준, F205의 날짜 범위 필터 동작과도 직결된다.
5. **F204 정렬 기준**: "최신 데이터가 상단"의 기준이 정제된 `timestamp` 필드인지, Firestore 문서 생성 시각(자동 생성 순서)인지 명시되어 있지 않다. 위 4번 항목(timestamp 파싱 실패)과 함께, 파싱 실패한 로그가 정렬 순서상 어디에 위치해야 하는지도 확인이 필요하다.
6. **F206 색상 값**: INFO/WARNING/CRITICAL 및 알 수 없는 레벨 각각에 적용할 구체적인 색상(텍스트/배경 hex 값 등)이 지정되어 있지 않다. 디자인 톤(다크테마 `#121214` 배경과 대비되는 색)만 확정되어 있고 정확한 팔레트는 없다.
7. **F200 홈 복귀 시 착지 화면**: 로그 관리 앱에서 인포테인먼트로 돌아갈 때, 항상 Home 뷰(`switchView('home')`)로 착지해야 하는지, 아니면 Setting 버튼을 누르기 직전에 보고 있던 뷰(Navi/Weather 등)로 복귀해야 하는지 명시되어 있지 않다.
8. **태블릿 대상 해상도/뷰포트**: 공식 명세서가 "태블릿 화면"을 명시하지만 구체적인 목표 해상도나 뷰포트 크기가 주어지지 않았다. Footer 고정 노출, 터치 타깃 크기 등 수용 기준 검증 시 기준 해상도가 필요하다.
9. **F207 UI 위치**: 사용자 ID 기준 선별 기능이 F205 필터 패널에 통합된 입력 필드(④ 영역)로 제공되어야 하는지, 별도의 독립된 화면/기능으로 제공되어야 하는지 명시되어 있지 않다.
10. **F202 중복/재실행 처리(멱등성)**: 동일 `raw_telematics_logs` 문서에 대해 트리거가 의도치 않게 두 번 이상 실행되는 경우(재시도 등) `telematics_logs`에 중복 문서가 쌓이는 것을 방지해야 하는지, 또는 이 시나리오는 범위 밖으로 취급해도 되는지 확인이 필요하다.

위 미확정 사항은 모두 세부 동작/스타일 값에 대한 것으로, 기능 목록(F200~F207)의 경계를 정하는 데는 영향을 주지 않았다 — 즉 기능 목록 자체를 추측으로 구성하지는 않았다. 다만 3번·4번·5번 항목은 F202/F203/F204/F205의 정확한 수용 기준(특히 경계 케이스 판정)에 직접 영향을 주므로, 해당 기능 구현에 착수하기 전에는 확인이 필요하다.

## 5. PM 확정 사항 (MVP 기본 결정)

10개 미확정 사항 모두 구현을 막는 스펙 경계 문제가 아니라 세부 동작/디자인 값이라 판단하여, PM이 아래와 같이 기본값을 확정한다(todo-cli 때와 동일하게 PM의 MVP 단순화 결정 — 사용자 재확인 불필요, 다만 언제든 사용자 요청으로 변경 가능):

1. **빈 줄 처리**: 공백만 있는 줄은 업로드 단계에서 걸러내고 문서를 생성하지 않는다(레퍼런스 코드의 기존 동작과 동일).
2. **업로드 UI 위치**: 시각화 화면(`index.html`)과 분리된 별도 페이지(`upload.html`)로 만들고, `index.html`의 사이드바 영역에서 링크로 연결한다.
3. **정규식 비매칭 줄 처리**: 해당 원시 문서는 건너뛰고(`telematics_logs`에 문서를 만들지 않음) `console.error`로 원본 줄을 로그에 남긴다(CODING_GUIDE §5 로깅 규칙 — 조용히 무시하지 않음).
4. **timestamp 파싱 실패 처리**: `new Date()` 결과가 유효하지 않으면(`isNaN(date.getTime())`) 원본 문자열을 그대로 `timestamp` 필드에 보존한다(Invalid Date 객체를 저장하지 않는다).
5. **정렬 기준**: `telematics_logs`를 `timestamp` 필드 기준 내림차순(`orderBy('timestamp', 'desc')`)으로 정렬한다. 파싱 실패로 문자열이 남은 극소수 항목의 정렬 위치는 Firestore의 기본 타입 정렬에 맡기고 별도 보정하지 않는다(알려진 한계로 문서화).
6. **F206 색상 팔레트**: 기존 `1649641/UI.md`의 팔레트를 그대로 재사용 — INFO는 Emerald Green(`#10B981`), WARNING은 Amber(`#F59E0B`, 신규 도입), CRITICAL은 Vivid Red(`#EF4444`) 배경 강조, 알 수 없는 레벨은 Text Muted(`#9CA3AF`) 기본 스타일.
7. **F200 홈 복귀 착지 화면**: 항상 인포테인먼트의 기본 진입 화면(`1649641/index.html` 로드 시 기본으로 뜨는 Home 뷰)으로 복귀한다. 직전 뷰 기억 기능은 범위 밖.
8. **태블릿 해상도**: 별도 수치를 새로 정의하지 않고, `1649641/index.css`가 이미 쓰는 태블릿 베젤/컨테이너 스타일 패턴을 그대로 재사용한다.
9. **F207 UI 위치**: 별도 화면을 만들지 않고 F205 필터 패널에 사용자 ID 입력 필드를 추가하는 형태로 통합한다.
10. **F202 멱등성**: 이번 범위에서는 중복 방지 로직을 구현하지 않는다(재시도로 인한 중복 생성 가능성은 알려진 한계로만 문서화).

## 6. 구현 소유권 분담 (파일 경로 배타적 분리)

- **implementer-A**: `pjt2/1649641/` 내부만 — F200 중 인포테인먼트 쪽(Setting 버튼 추가, 다른 경로 손대지 않음).
- **implementer-B**: `pjt2/1649641-pjt2/index.html`, `upload.html`, `styles.css`, `app.js`, `upload.js`, `env.js.example`, `.gitignore` — F200 중 로그 앱 쪽(홈 복귀 버튼), F201, F204, F205, F206, F207. `functions/`와 Firebase 프로젝트 설정 파일은 건드리지 않는다.
- **implementer-C**: `pjt2/1649641-pjt2/functions/**`, `pjt2/1649641-pjt2/firebase.json`, `.firebaserc`, `firestore.rules` — F202, F203. 프런트엔드 파일은 건드리지 않는다.

세 경로 집합은 서로 겹치지 않으므로 스크래치 디렉터리 격리 없이 배타적 경로 지정만으로 충돌을 방지한다.

## 7. 구현 현황 (Implementation Status)

- 통합 대상 코드: `pjt2/1649641/index.html`(implementer-A), `pjt2/1649641-pjt2/{index.html,upload.html,styles.css,app.js,upload.js,env.js.example,.gitignore}`(implementer-B), `pjt2/1649641-pjt2/{functions/**,firebase.json,.firebaserc,firestore.rules}`(implementer-C)
- 통합 담당: team-lead · 검증 담당: tester (2026-09-11) · 이번 라운드는 스크래치 디렉터리 격리 없이 3명의 구현 에이전트가 배타적 경로에 직접 최종 코드베이스로 작성함(PM 확인, write race 없음).
- 통합 노트:
  - `pjt2/1649641-pjt2/env.js`를 team-lead가 신규 생성(PM이 전달한 실제 Firebase 프로젝트 `pjt2-c3b41` config 값 사용). `env.js.example`의 키 이름과 PM이 전달한 값의 키 이름이 정확히 일치해 변환 없이 그대로 채움. `.gitignore`에 `env.js`가 이미 등록되어 있음을 확인(implementer-B 보고 그대로 정확).
  - implementer-B `app.js`/`upload.js`가 기대하는 `telematics_logs` 필드(`timestamp`/`user`/`log_level`/`message` + 가변 숫자 필드)와 implementer-C `functions/parseLog.js`의 실제 출력 필드명이 team-lead가 코드 대조로 직접 확인한 결과 정확히 일치함(계약 불일치 없음).
  - implementer-A의 `index.html` diff를 `git diff`로 직접 확인 — Setting 버튼 1개 추가 외에, 기존에 깨져 있던 `<script src="./index.js "></script>` 뒤 잉여 `</script>` 태그도 함께 정리됨(최소 수정 범위 내 부수적 수정으로 판단, 별도 재작성 없음).
  - team-lead가 `node --check`로 `app.js`/`upload.js`/`functions/parseLog.js`/`functions/index.js` 구문을 직접 확인하고, `functions/test/parseLog.test.js`를 독립적으로 재실행해 6/6 PASS를 직접 확인함(3단계 빌드/구동 확인).
  - **CODING_GUIDE §3 편차(보고만, 미수정)**: implementer-B가 `app.js`/`upload.js`의 코드 주석을 한국어로 작성함(§3은 "코드 자체와 개발자용 주석은 영어로 통일" 요구) — 실제 규칙 위반 확인. 최소수정원칙에 따라 team-lead가 임의로 전면 재작성하지 않았음. PM이 사용자와 상의해 그대로 둘지 고칠지 결정 필요.
  - **implementer-C 판단콜(보고만)**: `firestore.rules`에서 `raw_telematics_logs`의 `create`를 인증 없이(`allow create: if true`) 허용 — 이 프로젝트 범위에 인증 시스템이 없으므로 나온 판단이며 스펙과 모순되지 않음(F201 업로드 UI가 클라이언트에서 직접 `addDoc`하는 구조와 일치). 판단콜로만 기록.
  - **F201 tester 관찰(경계 케이스, team-lead/PM 확인 필요)**: `upload.js`가 `Promise.all`로 전 줄을 병렬 업로드해 Firestore 문서 *생성완료* 순서가 원본 줄 순서와 다를 수 있음(코드 주석에도 명시된 알려진 한계). 다만 F204 화면 정렬은 문서 생성 순서가 아니라 로그 줄 안에 내장된 `timestamp` 필드 기준(`orderBy('timestamp','desc')`)이라 실제 화면 표시 순서는 영향받지 않을 가능성이 높음 — 그러나 이는 F201 자체가 순서를 보장해서가 아니라 F204 정렬 방식에 결과적으로 의존하는 것이므로, AC6("나중 줄이 먼저 줄보다 먼저 처리된 것처럼 보이지 않아야")을 F201의 자체 보장으로 볼지 여부는 tester가 PASS로 채점했으되 PM 확인이 필요한 지점으로 남김.
  - **F202/F203 tester 관찰(경계 케이스, 실패 아님)**: `parseLog.js`의 음수 필터가 `num < 0`이라 `-0`은 걸러지지 않고 숫자 필드로 남음(스펙은 `-2.7` 같은 일반 음수만 예시로 듦, `-0` 명시 없음). 실패로 채점하지 않았으나 참고로 기록.
  - 라이브 Firebase(`pjt2-c3b41`) 네트워크 접근 및 브라우저 자동화 도구가 이 실행 환경에 없어, F200/F201/F204~F207의 실제 브라우저 렌더링·실시간 Firestore 왕복은 tester가 라이브로 검증하지 못했다 — 전부 정적 코드 리뷰 + (가능한 경우) 순수 로직을 Node로 직접 재현 실행하는 방식으로 대체 검증했으며, 각 tester 보고서에 이 한계가 명시되어 있다. F202/F203(`parseLog` 순수 함수)만 네트워크가 불필요해 tester가 실제 `node` 실행으로 독립 검증함(implementer-C 자기 보고를 그대로 신뢰하지 않음).
  - tester가 새로 작성한 회귀/검증 테스트 파일이 코드베이스에 남아있음: `pjt2/1649641-pjt2/test/upload.f201.test.js`, `pjt2/1649641-pjt2/functions/test/parseLog.f202f203.extra.test.js`, `pjt2/1649641-pjt2/test/f204_f207_logic.test.js`, `pjt2/1649641-pjt2/test/f204_f207_staticReview.test.js` (전부 프로덕션 파일 미수정, 신규 테스트 파일만 추가).
  - 파일 소유권 위반: 없음(3개 구현물 모두 배타적 경로 내에서만 작성 확인, `git diff`로 implementer-A 재확인).
  - 필명 충돌: 없음(경로 자체가 배타적).
  - 행동이 다른 중복 로직: 없음.

| ID | 기능명 | 상태 | 검증 방법 |
|----|--------|------|-----------|
| F200 | 인포테인먼트 Setting 진입 버튼 및 양방향 내비게이션 | ✅ 완료(PASS) | tester: 정적 코드 리뷰(자동 대조 스크립트 8건), 라이브 브라우저 렌더링/클릭 미검증(환경 제약 명시) |
| F201 | 원시 로그 데이터 수집 | ✅ 완료(PASS, 경계 관찰 1건 PM 확인 필요) | tester: 정적 코드 리뷰 + 순수 로직(줄 분리/필터링) Node 직접 실행 13건, 1000줄 합성 데이터 검증. 라이브 Firestore 왕복 미검증 |
| F202 | 실시간 데이터 정제 처리 | ✅ 완료(PASS) | tester: 기존 6건 독립 재실행 + 신규 10건 직접 작성/실행(Node), 총 16/16 PASS. exactly-once 트리거 실행 자체는 코드 리뷰로만 확인(라이브 트리거 미검증) |
| F203 | 구조화된 로그 데이터 저장 | ✅ 완료(PASS) | tester: 위 F202와 동일 테스트 스위트로 함께 검증(가변 스키마/4개 필수 필드/log_level 보존 등) |
| F204 | 로그 실시간 시각화 | ✅ 완료(PASS) | tester: 순수 로직 30건 + 정적 구조 검증 17건(Node 직접 실행). onSnapshot 실시간성/실제 렌더링은 라이브 미검증 |
| F205 | 데이터 상세 필터링 및 검색 | ✅ 완료(PASS) | tester: applyFilters AND 결합/경계값(자정 경계 등)을 Node로 직접 재현 검증, 위와 동일 스위트 |
| F206 | 로그 등급별 시각적 차별화 | ✅ 완료(PASS) | tester: CSS 색상 팔레트(4종 구분)·CRITICAL 최우선 시인성·미확인 레벨 기본 스타일을 정적 검증 |
| F207 | 사용자 맞춤형 로그 분석 (심화) | ✅ 완료(PASS) | tester: F205와 동일 `applyFilters` 내 AND 결합 확인, 사용자 미일치/빈 상태 케이스 포함 |

**tester 실행 로그**: RUN_LOG.md 참고 — F200/F201/F202+F203/F204~F207, 4개 그룹 호출 모두 1회차에 전건 PASS, 재호출(3-strikes) 불필요.
**미구현/커버리지 갭**: 없음(스펙의 모든 기능(F200~F207)에 대해 implementer 산출물이 존재하고 tester 검증을 통과함).
**3-strikes 도달 기능**: 없음.
