# 변경 이력 (Changelog)

버전 표기는 `0.MINOR.PATCH` 형식을 쓴다 (0.y.z: 아직 안정화 전 단계라는 semver 관례).

- **PATCH** (`0.0.x`): 기존 에이전트의 규칙/체크리스트 보강, 문서 수정, 버그 수정
- **MINOR** (`0.x.0`): 새 에이전트 추가, 팀 구조나 파이프라인 흐름의 변경
- **MAJOR** (`x.0.0`): 하네스가 "안정판"으로 간주될 때 (아직 해당 없음)

버전을 올리는 주체는 PM이며, 커밋 + 태그 직후 이 파일에 항목을 추가한다.

## [0.1.0] - 2026-09-14

하네스를 "특정 프로젝트에 종속되지 않는, 에이전트만을 위한 범용 인프라"로 확정한 릴리스. 이번
버전부터는 이 폴더를 어떤 프로젝트 루트에 복사해 넣어도, 프로젝트 잔재 없이 바로 쓸 수 있다.

### 추가됨

- **`tech-spec-writer` 에이전트** (`.claude/agents/tech-spec-writer.md`): 실제 코드베이스·
  레퍼런스 문서(기존 소스, 프로토콜/API 문서, 샘플 코드)를 직접 읽어 기술 설계 스펙을 작성.
  `spec-writer`는 코드를 전혀 안 읽는 원칙이라 커버하지 못했던 영역(외부 프로토콜/샘플 코드
  기반 작업) 보완
- **`ORCHESTRATION.md`에 "RUN_LOG.md 범위 규칙" 섹션**: 하네스 자체 로그와 프로젝트별 로그를
  명확히 분리하는 판단 기준 문서화

### 변경됨

- **`tester.md`**: "회귀 우선" 0단계 추가 — 새 테스트를 짜기 전에 기존 테스트부터 재실행하고,
  작성한 테스트는 삭제하지 않고 영구 회귀 자산으로 누적
- **`HANDOFF.md` 전면 재작성**: 특정 프로젝트 사본(`ppjt2`)의 진행상황 서술을 걷어내고, 하네스
  레벨 정보만 남김. "완료된 프로젝트 스펙도 매번 필독"하던 읽기 순서를 "이어서 할 프로젝트가
  있을 때만 그 프로젝트 스펙을 읽는다"로 수정
- **`ORCHESTRATION.md`/`CODING_GUIDE.md`**: 특정 프로젝트명(`ppjt2`)에 하드코딩되어 있던 문구를
  전부 프로젝트 무관 표현으로 일반화

### 제거됨

- 하네스 폴더 안에 섞여 있던 **프로젝트 인스턴스 전체**: `todo-cli/`(코드+SPEC.md+RUN_LOG.md),
  로그 데이터 관리 PJT 아카이브(`SPEC_LOG_PJT.md`, `16-C-1-02.pdf`), 깨진 gitlink `1649641`
  (서브모듈 미등록 빈 폴더). 원본/기록은 각 프로젝트 자신의 위치에 남아있거나 git 히스토리로
  복구 가능 — 하네스 자체에는 더 이상 프로젝트 콘텐츠를 두지 않는다는 원칙에 따른 정리

### 확인된 사항

- 이 하네스가 서로 무관한 세 프로젝트(할 일 관리 CLI, 로그 데이터 관리 웹, 당구 AI 소켓
  클라이언트)에 재사용되며 스펙-작성/구현/통합/검증 파이프라인 자체는 유효함이 반복 확인됨 —
  다만 각 프로젝트의 성격(코드 의존도, 시간 제약, 실측 필요 여부)에 따라 PM이 파이프라인
  전체를 태울지, 일부만 태울지 유연하게 판단해야 함도 함께 확인됨(PM이 소스 직접 작성한 사례
  발생 — hollinone 프로젝트, 사유는 그쪽 프로젝트의 `RUN_LOG.md` 참고)

## [0.0.1] - 2026-09-11

첫 태깅 버전. PM–team-lead–tester 3단 구조로 spec부터 통합·검증까지 한 사이클을 실제로
돌려본 첫 안정 상태.

### 추가됨

- **에이전트 5종** (`.claude/agents/`): `agent-builder`, `doc-writer`, `spec-writer`,
  `team-lead`, `tester`
- **`ORCHESTRATION.md`**: 팀 구조, PM의 권한 경계, 루프/폭주 방지 정책(3-strikes, RUN_LOG.md
  기반 기계적 카운트), 이상 감지 책임 분담(계층별), 구현 에이전트 디스패치 원칙(파일 소유권 +
  스크래치 디렉터리 격리), 병합 품질 체크리스트, 새 에이전트 추가 절차
- **`CODING_GUIDE.md`**: 최소 수정 원칙, 네이밍/주석 규칙, 에러 처리 규칙, 로깅 규칙(앱 자체
  로그, RUN_LOG.md와는 별개), 시크릿(.env) 관리 규칙
- **`RUN_LOG.md`**: 에이전트 디스패치 기계적 로그 (루프 감지용)
- **첫 시험 운행 결과물**: `SPEC.md` + `todo-cli/`(할 일 관리 CLI, F1~F6 전 기능 PASS) —
  spec-writer → PM 확인 → 구현 에이전트 병렬 실행 → team-lead 통합 → tester 검증까지
  파이프라인 전 구간을 실제로 검증

### 확인된 사항

- team-lead → tester 위임이 `subagent_type: "general-purpose"` + 역할 정의 파일 지정 방식의
  우회로 이 harness에서 실제로 동작함을 확인 (네이티브 커스텀 서브에이전트 타입은 인식 안 됨)

### 알려진 한계

- 스크래치 디렉터리 격리·병합 품질 체크리스트(계약 대조/최소수정/시크릿 스캔)는 설계만 되어 있고
  실전 시험 운행은 아직 안 함
- "이 파일 건드리지 마라" 류 경계는 기술적 강제가 아니라 프롬프트 기반 소프트 제약
