# 코딩 가이드

이 문서는 이 하네스로 작업하는 프로젝트에서 만들어지는 **코드 자체**가 지켜야 할 규칙이다
(`ORCHESTRATION.md`가 "에이전트가
어떻게 행동하는지"라면, 이 문서는 "그 결과물인 코드가 어떤 모양이어야 하는지"). PM은 구현 에이전트를
디스패치할 때 SPEC.md, 인터페이스 계약과 함께 이 문서 경로를 항상 같이 전달한다.

## 1. 최소 수정 원칙 (재사용 우선)

- 기존에 동작하는 코드가 있다면, 새 기능을 위해 그 코드를 통째로 다시 쓰지 않는다. 필요한 최소
  범위만 수정/추가한다.
- 기존 함수·모듈이 필요한 기능을 이미 (거의) 제공한다면, 새로 만들지 않고 재사용하거나 확장한다.
- "더 깔끔해 보여서" 같은 이유만으로 관련 없는 코드를 리팩터링하지 않는다 — 그건 별도로 명시적
  요청이 있을 때만 한다.
- 위반 여부는 team-lead의 병합 품질 체크리스트(2a)에서 확인한다.

## 2. 네이밍 규칙

- 변수/함수: camelCase (예: `taskList`, `addTask`)
- 상수: UPPER_SNAKE_CASE (예: `MAX_TITLE_LENGTH`)
- 파일명: camelCase (예: `taskStore.js`, `userProfile.js`)
- 이름은 축약보다 명확함을 우선한다 (`tsk`보다 `task`)

## 3. 주석/문서화 규칙

- 모든 공개(export) 함수는 상단에 한 줄 요약 주석 — 무엇을 하는지, 매개변수와 반환값.
- "왜(why) 이렇게 했는지" 설명이 필요한 부분에만 주석을 단다 — 코드가 이미 보여주는 것(what)은
  주석으로 반복하지 않는다.
- 사용자에게 보이는 문자열(에러 메시지, 메뉴 텍스트 등)은 한국어, 코드 자체와 개발자용 주석은
  영어로 통일한다.

## 4. 에러 처리 규칙

- 예상 가능한 실패(잘못된 입력, 존재하지 않는 항목 등)는 항상 명확한 메시지의 Error를 던지거나
  반환한다 — 조용히 무시하거나 `undefined`를 반환하지 않는다.
- 사용자에게 보이는 에러 메시지는 원인을 담는다 (가능하면 해결 방법도).
- 예외 상황에서 프로그램이 죽지 않고, 스펙에 명시된 대로 복구 가능한 상태로 돌아가야 한다.

## 5. 로깅 규칙 (애플리케이션 로그 — RUN_LOG.md와는 다른 것)

**주의**: 이건 에이전트 호출을 기록하는 `RUN_LOG.md`(오케스트레이션 로그)와 완전히 다르다.
여기서 말하는 건 **만들어지는 앱 자체가 실행 중에 남기는 로그**다.

- 예외/에러가 발생하면 최소한 다음을 콘솔에 남긴다: 발생 시각, 어떤 동작 중이었는지, 에러 메시지.
- 주요 상태 변경 동작(추가/완료/삭제 등)은 성공 시에도 한 줄 로그를 남긴다 — 사용자에게 보여주는
  안내 메시지와는 별개로, 나중에 문제를 추적할 수 있도록.
- 로그 포맷은 프로젝트마다 team-lead가 통합 시점에 한 번 정하고(예: `[시각] [레벨] 메시지`),
  이후 일관되게 유지한다.

## 6. 비밀정보(시크릿) 관리

- API 키, 비밀번호, 토큰 등은 **절대 코드에 하드코딩하지 않는다** (예: 과거 프로젝트의
  `index.js`가 카카오맵·날씨 API 키를 `window.APP_CONFIG`로 다뤘던 사례처럼, 실제 프로젝트에서
  바로 마주치는 문제다).
- 환경변수 또는 `.env` 파일로 분리하고, 코드에서는 `process.env.XXX` 형태로만 참조한다.
- `.env` 파일은 **절대 버전 관리(git)에 포함하지 않는다** — 프로젝트 루트의 `.gitignore`에
  반드시 등록한다.
- 대신 `.env.example`처럼 **키 이름만 있고 값은 비어있는 템플릿 파일**을 만들어서, "이런
  환경변수가 필요하다"는 걸 문서로 남긴다 (실제 값은 절대 넣지 않는다).
- 로그(§5)에도 시크릿 값 자체를 남기지 않는다 — 설정됐는지 여부, 혹은 마스킹된 값만 남긴다.
- 실수로 시크릿이 코드나 커밋에 들어갔는지는 team-lead가 병합 품질 체크리스트(2a)에서, 로그에
  새어 나갔는지는 tester가 로깅 검증에서 함께 확인한다.

## 이 문서를 누가, 언제 참고하나

- **PM**: 구현 에이전트 디스패치 시 SPEC.md·인터페이스 계약과 함께 이 문서를 항상 같이 전달한다.
- **team-lead**: 병합 품질 체크리스트(2a)의 스타일 일관성·최소 수정 원칙 준수 여부를 이 문서를
  기준으로 판단한다.
- **tester**: 에러 처리 규칙이 실제로 지켜지는지(예외 상황에서 명확한 에러가 나는지), 로깅
  규칙이 지켜지는지(에러·주요 동작에 로그가 남는지)를 검증 항목에 포함한다.
